export default function LoadingSkeleton() {
    return (
      <div style={{ padding: '20px', textAlign: 'center' }}>
        <p>Loading...</p>
        {/* You can add a more complex skeleton structure here if needed */}
      </div>
    );
  }